# ============================================================
# GenAI Nexus — Local Execution Requirements
# Everything needed to run end-to-end locally with Ollama
# No cloud API keys required
# ============================================================

# --- Core ---
openai>=1.0                     # Also used for Ollama OpenAI-compat endpoint
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
requests>=2.31

# --- App UI ---
streamlit>=1.33
rich>=13.7
typer>=0.12

# --- LangChain + Ollama ---
langchain>=0.2
langchain-community>=0.2
langchain-ollama>=0.1           # Native Ollama support for LangChain
langchain-openai>=0.1           # Fallback: OpenAI client → Ollama endpoint
langgraph>=0.1

# --- LlamaIndex ---
llama-index>=0.10

# --- Embeddings + VectorDB ---
chromadb>=0.4
sentence-transformers>=2.7      # Local embeddings (all-MiniLM-L6-v2)

# --- Agents ---
crewai>=0.30
pyautogen>=0.2

# --- Safety ---
guardrails-ai>=0.4

# --- NLP ---
spacy>=3.7
nltk>=3.8

# --- HuggingFace ---
transformers>=4.40
datasets>=2.18
accelerate>=0.29

# --- ML Training (enables full mode) ---
torch>=2.2
keras>=3.0
scikit-learn>=1.4
numpy>=1.26

# --- Fine-Tuning (optional, needs GPU) ---
peft>=0.10
trl>=0.8
# bitsandbytes>=0.43             # Uncomment for GPU quantization (Linux/CUDA)

# --- Cloud LLM Providers (optional — not needed for local) ---
# anthropic>=0.30
# google-generativeai>=0.7

# --- AWS (optional — not needed for local) ---
# boto3>=1.34
# sagemaker>=2.214

# --- Inference Servers (optional) ---
# vllm>=0.4                      # Linux + NVIDIA GPU only
