# Starter: Dataflow Batch GCS -> BQ
- Goal: simple batch pipeline reading GCS files to BQ.
- Steps: template Java/Python; window default; set worker machine; launch; monitor job.
- Validation: rows ingested; job succeeds; cost note.
