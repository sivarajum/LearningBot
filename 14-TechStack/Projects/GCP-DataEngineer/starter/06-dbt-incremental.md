# Starter: dbt Incremental Model on BigQuery
- Goal: create incremental model with tests and docs.
- Steps: seed staging table; write incremental model; add tests (unique/not null/freshness); generate docs.
- Validation: dbt test passes; docs build; incremental run idempotent.
