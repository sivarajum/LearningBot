# Intermediate: Composer CI + DAG promotion
- CICD for DAGs with tests/lint; deploy to Composer; use connections/secrets; add data quality step.
- Validate: pipeline passes; DAG promoted; dq step enforced.
