# Starter: RAG Service on Cloud Run
- Goal: deploy simple RAG API with embeddings + vector DB.
- Steps: choose embed model; load sample docs; build retrieval; deploy; add rate limits.
- Validation: answers grounded; latency acceptable; logs captured.
