# Intermediate: CI/CD for Models
- Cloud Build pipeline: lint -> unit tests -> train (small) -> evaluate -> compare to baseline -> register if better.
- Validate: gate on metrics; model version tagged; artifacts stored.
