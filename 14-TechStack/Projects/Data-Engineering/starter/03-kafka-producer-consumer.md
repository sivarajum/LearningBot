# Starter: Kafka Producer + Consumer
- Goal: send/consume messages with keys and partitions.
- Steps: create topic; producer with batching/compression; consumer with group; commit offsets.
- Validation: messages delivered; offsets commit; latency measured.
