# Starter: dbt Incremental Model
- Goal: incremental model with tests and docs (warehouse-agnostic).
- Steps: seed data; incremental model; tests unique/not null; docs generate.
- Validation: dbt test passes; docs built; incremental run idempotent.
