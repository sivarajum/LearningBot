# Starter: Cloud Run Service
- Goal: deploy minimal REST service with env config.
- Steps: build image; deploy with min/max instances and concurrency; set env/secret; add health check.
- Validation: endpoint responds 200; revision rollout recorded; cost notes.
