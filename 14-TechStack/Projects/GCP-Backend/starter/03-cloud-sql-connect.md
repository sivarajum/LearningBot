# Starter: Cloud SQL Secure Connect
- Goal: connect Cloud Run to Cloud SQL with private IP/IAM auth.
- Steps: enable SQL IAM auth; create connector; set DB user; update service with instance connection.
- Validation: query succeeds; no public IP; principle of least privilege.
