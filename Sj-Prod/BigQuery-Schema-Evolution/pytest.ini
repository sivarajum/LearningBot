[pytest]
markers =
    unit: Unit tests (no external dependencies; run offline)
    integration: Integration tests (requires GCP credentials and --bq-project flag)
addopts = -m "not integration" --cov=src --cov-fail-under=80
testpaths = tests
